# npm 12 refuses git-protocol dependencies by default. indexeddbshim
# depends on sync-promise from GitHub, pinned to a commit in
# package-lock.json, so allow git specs anywhere in the tree ("root" would
# only cover our own direct dependencies). Ignored by npm 11 and older.
allow-git=all
